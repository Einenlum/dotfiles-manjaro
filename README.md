# Dotfiles (manjaro + i3)

## Usage

```
sudo pacman -Sy && \
sudo pacman -S git --noconfirm && \
git clone https://github.com/Einenlum/dotfiles-manjaro.git ~/dotfiles && \
cd ~/dotfiles && \
./install.sh
```
